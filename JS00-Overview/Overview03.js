/*
Cho 2 chuỗi, firstName và lastName, In ra 1 chuỗi mới với định dạng "firstName, lastName".
Đầu vào: firstName và lastName
Đầu ra: In ra 1 chuỗi mới với định dạng "firstName, lastName".
*/

function run() {
    var firstName = "Tuan";
    var lastName = "Phong";
    var name = firstName + ", " + lastName; // Toán tử + đươc sử dụng cho việc nối chuỗi trong javascript nếu ít nhất 1 trong các chuỗi là chuỗi.
    console.log(name);
}